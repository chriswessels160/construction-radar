name: Update Construction Projects

on:
  schedule:
    - cron: "0 12 * * *"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update-projects:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Run project updater
        run: python scraper.py

      - name: Update public-agency bids
        continue-on-error: true
        run: python bids.py

      - name: Commit updated projects
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add projects.json bids.json
          git diff --cached --quiet || git commit -m "Automatic construction data update"
          git push
